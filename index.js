import {
    addTapListener as TL
} from "./addTapListener"
import {
    deepValue as DV
} from "./deepValue"
import {
    connect as CN
} from "./connect"

export const addTapListener = TL
export const deepValue = DV
export const connect = CN